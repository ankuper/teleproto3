# server/ Changelog

Notable changes to the MTProxy fork. Follows [SemVer](https://semver.org/)
with prefixed tags (`server-v0.1.0`, ...). Each release records the
`lib-vX.Y.Z` it links against via the `linked_lib` field in `VERSION`.

## [Unreleased]

### Added
- Scaffold: root meta (README, UPSTREAM.md, CHANGELOG, VERSION,
  Makefile, Dockerfile), fork-local stubs under `net/net-type3-*`,
  test harness skeletons, runbook placeholders.

## [server-v0.1.0-draft] — 2026-04-24

Draft initialisation. Subtree pull from upstream MTProxy not yet
performed; server does not build until that lands.
