# fragmentation.yaml — WebSocket frame fragmentation vectors.
# Validates that an IUT correctly reassembles arbitrarily fragmented
# frames per RFC 6455 §5.4 + spec/wire-format.md §2.
#
# Pinned:
#   spec: spec-v0.1.0-draft
#   lib:  lib-v0.1.0-draft
#
# TODO(conformance-v0.1.0): populate scenarios. Seed shape below.

schema_version: "1.0"

scenarios: []
# - id: frag-basic-two
#   description: "A single application message split across two WS frames."
#   input_frames:
#     - { fin: false, opcode: 2, payload_hex: "..." }
#     - { fin: true,  opcode: 0, payload_hex: "..." }
#   expect:
#     reassembled_hex: "..."
